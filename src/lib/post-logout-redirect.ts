/**
 * Where users land after an intentional logout (portal /logout, admin sign out).
 * Admin-auth failures still pass ?redirect=/login explicitly.
 */
export const POST_LOGOUT_EXTERNAL_URL = "https://www.omniflow.com";
